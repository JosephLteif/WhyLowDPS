{
  "name": "whylowdps",
  "version": "0.2.4",
  "private": true,
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@tauri-apps/plugin-updater": "^2",
    "cross-env": "^7"
  },
  "scripts": {
    "tauri": "tauri",
    "web": "docker compose up --build",
    "web:dev": "docker compose -f docker-compose.dev.yml up --build",
    "backend:dev": "cd backend && cargo watch -w core/src -w server/src -w core/Cargo.toml -w server/Cargo.toml -w Cargo.toml -x 'run -p whylowdps-server'",
    "kill": "node scripts/kill-ports.js",
    "desktop:dev": "cd desktop && cross-env DESKTOP_BUILD=true NEXT_PUBLIC_DESKTOP_BUILD=true tauri dev --config src-tauri/tauri.conf.json",
    "desktop:build": "cd desktop && cross-env DESKTOP_BUILD=true NEXT_PUBLIC_DESKTOP_BUILD=true tauri build --config src-tauri/tauri.conf.json"
  }
}
