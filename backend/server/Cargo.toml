[package]
name = "whylowdps-server"
version.workspace = true
edition = "2021"

[features]
default = []
desktop = ["whylowdps-core/desktop"]
web = ["whylowdps-core/web"]

[dependencies]
whylowdps-core = { path = "../core" }
tokio = { version = "1", features = ["full"] }
